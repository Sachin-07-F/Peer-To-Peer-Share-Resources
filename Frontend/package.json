{
  "scripts": {
    "build": "node build.js",
    "start": "serve public",
    "dev": "npm run build && npm start"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "serve": "^14.0.1",
    "socket.io": "^4.8.1"
  }
}
